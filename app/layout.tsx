import "./globals.css";
import type { Metadata } from "next";
import Navbar from "../components/Navbar";
import Footer from "../components/Footer";

export const metadata: Metadata = {
  title: "Rayyan Air | Luxury Private Charters & Silk Road Expeditions",
  description: "Experience the world's most dramatic mountain corridors with Rayyan Air. Private charters to Northern Pakistan and Xinjiang along the historic Silk Road.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
